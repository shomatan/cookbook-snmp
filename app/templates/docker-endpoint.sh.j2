#!/bin/sh

/usr/bin/wp core download --allow-root --locale=ja --path=/var/www/wordpress

chown -R webuser:webuser /var/www/wordpress
chmod -R 700 /var/www/wordpress

exec /usr/bin/dumb-init php-fpm -F
